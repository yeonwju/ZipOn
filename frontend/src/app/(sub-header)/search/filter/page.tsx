import SearchFilterPageClient from '@/app/(sub-header)/search/filter/SearchFilterPageClient'

/**
 * 검색 필터 페이지
 *
 * 검색 필터를 설정하는 페이지입니다.
 * 아래에서 위로 슬라이드 애니메이션으로 표시됩니다.
 */
export default function SearchFilterPage() {
  return <SearchFilterPageClient />
}

